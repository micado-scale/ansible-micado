user_id: userid
infra_name: micado_worker_infra

nodes:
- &Worker_cluster 
  name: worker
  type: worker
  scaling:
    min: 1
    max: 1

variables:
  master_host_ip: {{ ansible_default_ipv4.address }}
